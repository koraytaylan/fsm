# git-cliff configuration: generates CHANGELOG.md and the release notes from
# the conventional-commit history during the tag-triggered release workflow
# (.github/workflows/release.yml). Nothing here is committed to the repository;
# both files are produced at release time and attached to the GitHub release.

[changelog]
header = """
# Changelog

All notable changes to fsm, generated from its commit history.
"""
body = """
{% if version %}\
## [{{ version | trim_start_matches(pat="v") }}] - {{ timestamp | date(format="%Y-%m-%d") }}
{% else %}\
## [Unreleased]
{% endif %}\
{% for group, commits in commits | group_by(attribute="group") %}
### {{ group | striptags | trim | upper_first }}
{% for commit in commits %}
- {{ commit.message | upper_first | split(pat="\n") | first }}\
{% endfor %}
{% endfor %}\
"""
trim = true

[git]
conventional_commits = true
filter_unconventional = false
commit_parsers = [
  { message = "^feat", group = "<!-- 0 -->Features" },
  { message = "^fix", group = "<!-- 1 -->Fixes" },
  { message = "^perf", group = "<!-- 2 -->Performance" },
  { message = "^refactor", group = "<!-- 3 -->Refactoring" },
  { message = "^docs", group = "<!-- 4 -->Documentation" },
  { message = "^test", group = "<!-- 5 -->Testing" },
  { message = "^chore", group = "<!-- 6 -->Chores" },
  { message = ".*", group = "<!-- 7 -->Other" },
]
tag_pattern = "v[0-9].*"
sort_commits = "oldest"
